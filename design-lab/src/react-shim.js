// React comes from cdnjs (UMD globals); this shim lets the sources use normal imports.
const R = window.React;
export default R;
export const { useState, useEffect, useLayoutEffect, useRef, useMemo, useCallback, useContext, createContext, Fragment, forwardRef, memo, useReducer, useId } = R;
