{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-resize",
  "description": "Resize the settings window from its corner grip",
  "windows": [
    "settings"
  ],
  "permissions": [
    "core:window:allow-start-resize-dragging"
  ]
}
