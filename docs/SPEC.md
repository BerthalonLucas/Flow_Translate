# FlowTranslate V1

## Confirmed scope

Windows 11; French/English professional email and messages; a private pilot of 1–10 people. The Windows client and vLLM server are separate products, initially exercised on Lucas's PC. No voice, OCR, rich Office formatting or whole-document translation.

## Approved design

> Nouvelle direction artistique « Îlot » (24 septembre 2026), en cours d’implémentation sur la branche `da-ilot` : plan, lots et valeurs exactes dans [docs/DA-PLAN.md](DA-PLAN.md), référence visuelle et de mouvement dans le labo [design-lab/](../design-lab/README.md). Cette section décrit la cible, pas des fonctions livrées. Les choix encore à confirmer par Lucas sont dans la dernière section de [UI-DECISIONS.md](UI-DECISIONS.md) ; l’ancienne conception est en fin de document (« Historique »).

Select text, press the menu shortcut: the Îlot, a small action menu, opens beside the selection. At rest it shows the last action (Enter runs it again) and a pastille for a free instruction; Tab, ↓ or hovering unfolds a 3×2 grid of tiles (by default Fix, Translate, Pro, Shorten, Email, and the free instruction while fewer than six actions exist). Letters (F T P S E), digits 1–6, Space, `/` or an unassigned letter act from the keyboard. The chosen action replaces the selected text in place. No header, oversized button or footer.

While the model works, the menu morphs into a pill (28 px high, 44 px wide) carrying the Perle orb, shown after 250 ms; no text, and the pill never grows. Where the application exposes its selection through UI Automation, a light sweep is drawn over the selected lines by a separate, never clickable window (`halo`); FlowTranslate never recolours the source's letters. After the paste: a traced check, an Undo button with an 8 s countdown (paused on hover), and the changed words highlighted while Undo is available; the pill moves under the new text and never covers it. Each of these can be switched off in Settings. Errors are classified: a configuration error (address, key, model, rights) offers a button that opens the exact Settings field; a transient error offers Retry; a refused paste or a changed selection replaces nothing and offers the result to copy.

The theme follows Windows: light glass in light mode, dark glass in dark mode. A WebView2 window cannot blur the desktop, so the default material is painted glass with raised opacity (plan, lot 12 phase A); real Windows Acrylic is only a trial behind a hidden setting, for Lucas to decide. Icons: Lucide, stroke 1.5. The interface is in English by default, with a complete French toggle. Motion uses Apple's « smooth » spring preset (« bouncy » as an option); content stays centred while a surface changes shape. A setting « Animations: follow Windows / always / reduced » replaces the implicit OS preference; reduced keeps only short opacity fades, a still orb and a still veil instead of the sweep.

The « Show result » mode (no replacement) and long translations keep their behaviour (short glass near the selection, reading band at the bottom of the cursor's screen), restyled in the new material, icons and motion (plan, lot 11). Idle means hidden except the tray icon. Settings follow the same theme at a readable normal window size and accept direct links from errors to a field.

Use Motion/Radix/Lucide for accessible interactions. The overlay window is reserved once for the largest form; inside it the DOM animates the surface's width, height and radius, and no native resize (`SetWindowPos`) happens during an animation (plan §4.3). Native hit-testing must follow the actual surfaces; transparent margins must not intercept desktop clicks. Closing cancels immediately and permits a short bounded exit animation, with a native fallback if the frontend does not acknowledge. The lab and mockups do not establish native blur, focus behaviour or performance.

## Interaction

Ctrl+Alt+Space (setting string `Ctrl+Alt+Space`), configurable, opens the Îlot. No Ctrl+Alt+letter combination is offered by default: Ctrl+Alt is AltGr on AZERTY, and the shortcut recorder flags combinations that collide with AltGr on the active layout. A binding targets either the menu or one action directly. Pressing the menu shortcut twice within 400 ms reruns the last action, remembered per source application (process name → action id, never text). Capture: read the selection via UI Automation, with the rectangle of every selected line; else copy it for the user (synthetic Ctrl+Insert once the chord is released, clipboard restored); else accept a copy the user made less than three seconds before; else a short notice, never a modal dialog. Without selection rectangles nothing is drawn over the text and the pill alone opens at the bottom of the cursor's screen. Selection coordinates determine placement; clamp to the monitor working area and flip above when needed. The overlay takes keyboard focus only while the menu is open and gives it back to the source before pasting; afterwards it never activates, so clicking Undo does not steal focus. Escape in the menu closes it and returns focus to the source without pasting; later, Escape dismisses/cancels. Scroll/window/selection changes invalidate replacement and stale anchoring. Result actions become active only after a complete response. Never paste into an unverified or changed target. Undo sends Ctrl+Z to the source by default, only after revalidating that the source is in the foreground and the pasted text is still in place, and disappears as soon as the user types in the source; the alternative (reselect the new text and paste the original back, same revalidation) is a setting. Clipboard restoration must not overwrite newer user content.

## Client and server

Tauri 2 + React/TypeScript; Rust owns native capture, windows, network, storage and secrets. React communicates only through the typed bridge. Browser preview provides deterministic mock responses and is clearly identified as a demo. Real desktop mode is the default for the packaged app.

vLLM 0.28.0 stable, pinned image and model revisions. Fast: tencent/Hy-MT2-1.8B BF16. Quality: tencent/Hy-MT2-7B-FP8. Two independent endpoints and no implicit profile fallback. OpenAI /v1/models and /v1/chat/completions with streaming. 8192-token initial context, 6000-character client limit, 4096 output token budget; reject oversized context and incomplete generations explicitly. Official templates and generation parameters must be recorded. These remain candidates pending actual GPU and quality measurements. Since 0.4.0 the delivered server also has a `general` profile (Gemma 4 12B QAT with speculative decoding) for correcting and rewriting: the Hy-MT models only translate (README.md).

Defaults: English interface, menu on Ctrl+Alt+Space, theme following Windows, « smooth » motion, Perle indicator, Quality mode, no autostart, history off. The French target is no longer a default: since 0.4.0 the language lives in each action's instruction. Optional local history: Windows DPAPI, 7 days/100 entries, delete individual/all. No content logging. Remote endpoints require HTTPS except loopback. Credentials remain on the machine.

## Delivery and evidence

First milestone: native Tauri visual prototype and simulated responses. Then real inference integration. Test cancellation, stale streams, clipboard races, focus/selection changes and network failures. Test Edge/Chrome/Word/Outlook/Teams manually; track unrun cases honestly. Design checks at 100/125/150/200% scaling and reduced motion. Evaluate 100 balanced FR/EN excerpts, fidelity/names/numbers/format and latency p50/p95 at 1/4/10 concurrent requests. Do not fabricate model results or human scores. Package Windows installer, separate reproducible server and rollback instructions.

All code/config/docs/test data and selected design references are versioned in private GitHub repo BerthalonLucas/flowtranslate. Never commit confidential examples, model weights or secrets.

## Historique : conception du 9 septembre 2026 (bulle graphite, jusqu’à la 0.4.0)

Remplacée par la DA Îlot du 24 septembre 2026 (docs/DA-PLAN.md, design-lab/). Texte d’origine, gardé pour mémoire. Il était déjà en partie dépassé avant la DA : la capsule n’est plus affichée depuis le 10 septembre 2026, le raccourci ne donne plus le focus à une bulle ouverte, et depuis la 0.2.0 le code peint un seul graphite `rgba(24,26,31,.96)` sans matériau natif (docs/BRIDGE.md).

> On 2026-09-09 Lucas approved `design/glass-reader/c-overlapping-pill.png`, superseding the earlier selection/clipboard mockups for the translation surface. A short selection gets a 280 logical-pixel smoked-graphite glass bubble near its last visible character, with 26px corners, 15px text and compact padding. The translation surface contains only text; Copy and More live in a small horizontal pill partly overlapping the upper-right edge, without covering the first text line. The 0.1.4/0.1.5 calibration uses a 66% graphite background on the main glass and 68% on accessories, with native acrylic, a faint rim/reflection and understated shadow; text stays fully opaque. No header, oversized button or footer band.
>
> Long translations use a wider reader (approximately 560 logical pixels) at the bottom center of the selection's monitor work area, with the same overlapping action pill. No visible scrollbar. Preserve the complete translation and keyboard/wheel access for content exceeding the reader. Estimate the presentation before streaming; avoid per-token window movement and allow at most one promotion if the translated text exceeds the initial estimate. Clipboard confirmation and the 200×36 capsule remain. Idle means hidden except tray icon. Settings use the same materials at a readable normal window size.
>
> Use Motion/Radix/Lucide for accessible interactions and short paint animations. Honor reduced motion. Native window clipping must follow the actual glass/pill/menu surfaces; transparent margins must not intercept desktop clicks. Closing cancels immediately and permits a short bounded exit animation, with a native fallback if the frontend does not acknowledge. Mockups do not establish native blur or performance.
>
> Ctrl+Alt+T, configurable: read a selection via UI Automation; else copy it for the user (synthetic Ctrl+Insert once the chord is released, clipboard restored) ; else accept a copy the user made less than three seconds before; else a short notice, never a modal dialog. Selection coordinates determine placement; clamp to monitor working area and flip above when needed. No reliable anchor means bottom capsule. Preserve foreground focus when showing an overlay. Repeated shortcut focuses it. Escape dismisses/cancels. Scroll/window/selection changes invalidate replacement and stale anchoring. Result actions become active only after a complete response. Never paste into an unverified or changed target. Clipboard restoration must not overwrite newer user content.
>
> Defaults: French target, Quality mode, no autostart, history off.
