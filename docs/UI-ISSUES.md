# Revue UI — registre des défauts

Un défaut par correction. Statuts : à reproduire → reproduit → corrigé → vérifié.
Une compilation ou une référence visuelle inchangée ne valide pas l’esthétique.

| ID | Défaut / état actuel | Reproduction | Critère de correction | État |
|---|---|---|---|---|
| UI-001 | Surface grise résiduelle / fermeture native rapportées par Lucas | Afficher, ouvrir le menu, fermer et recommencer sur fonds clair/sombre | Plus de surface visible ni de zone bloquant la souris après fermeture ; vérifier aussi pendant annulation | Reproduit sur 0.1.5 (`native-before`, HWND encore visible après Fermer) ; cause établie : `window.hide()` sans effet après un `SetWindowPos(SWP_SHOWWINDOW)` hors Tao ; correctif `host::hide` (depuis le 2026-09-10 : `ShowWindow(SW_HIDE)` seul, sans passer par Tao) **vérifié** par le probe renforcé sur le build du 2026-09-09 (`release/ui-evidence/native-after-1a/result.json` : 3 cycles, HWND overlay et capsule masqués après Fermer ; `native-after/result.json` reste l’échec de préparation antérieur). Composition du bureau toujours non mesurée : capture GDI et Windows-MCP refusées dans cette session (« Descripteur non valide »), comme pour Codex |
| UI-002 | Capsule sans repli au survol | Atelier → Capsule actuelle | Trait au repos ; langue/action/menu au survol ; aller vers le menu sans fermeture prématurée ; déplacement et aimantation | Remplacée le 2026-09-10 par l’onglet du verre replié (UI-009) ; la fenêtre capsule n’est plus affichée |
| UI-003 | États et transitions de la bulle à reprendre | Atelier → court, attente, interruption, erreur | Lecture stable ; actions seulement à la fin ; Échap ferme ; résultat persistant hors survol | Refonte « 1a » implémentée (2026-09-09, handoff design) : verre 300 px texte seul, pilule opaque mordant le bord, coche 1,6 s, menu ancré sous la pilule. Vérifiée dans le navigateur et par les 44 tests ; rendu Windows (acrylique, contraste sur page blanche, DPI) à valider par Lucas |
| UI-004 | Lecture longue et barre de défilement | Atelier → long ; molette/clavier | Tout le texte accessible, barre discrète lors du défilement | Implémenté selon le handoff : croissance ligne à ligne jusqu’à 220 px, défilement interne, fondus de bord, indicateur 3 px au survol/défilement (800 ms), chip « la suite arrive », Agrandi 420 × 440 depuis le coin ancré. Le panneau bas centré n’existe plus pour une sélection ancrée ; à revalider visuellement par Lucas |
| UI-005 | Réglages : fond incomplet, thème et historique peu lisibles | Atelier → réglages/historique ; 900 puis 480 px | Toute la fenêtre cohérente ; thème Windows ; réglages et anciennes traductions accessibles | Fenêtre 520 px graphite opaque sans cadre système, enregistrement automatique, raccourci par capture, connexion lisible (handoff design). Thème clair Windows non traité : la maquette est sombre uniquement. Fenêtre native (hauteur au contenu, déplacement par la barre) à vérifier sur le build |
| UI-006 | Capture Windows automatique indisponible | Computer Use → démo native | Capture du bureau exploitable et interactions natives mesurables | Bloqué côté outil : capture noire puis GetCursorPos 0x80070005 ; CDP fonctionne |
| UI-008 | Cadre gris derrière la bulle et la capsule (retour de Lucas, 2026-09-10, capture d’écran) | Capture réelle → observer le rectangle clair autour du verre arrondi | Aucune surface hors de la silhouette arrondie | Cause établie par la doc DWM : `DWMWA_SYSTEMBACKDROP_TYPE` peint le matériau derrière toute la fenêtre, la région ne le découpe pas ; tout matériau apparaît aussi sous les fondus. Correctif : plus de matériau natif (`host::apply_glass`, `FLOWTRANSLATE_GLASS` pour expérimenter), verre `.92` peint par le DOM. À valider sur le bureau (capture après reconstruction) |
| UI-009 | La bulle reste ouverte quand la souris sort ; Lucas veut un repli en tout petit en bas de l’écran et une réouverture au survol | Traduire, sortir le pointeur ; survoler l’onglet | Repli en onglet 44 × 20 px au bord bas 500 ms après la sortie (10 s sans visite) ; survol de l’onglet → verre au-dessus ; menu ⋯ ou × de l’onglet pour fermer | Implémenté (`presentation: 'docked'`, fenêtre ancrée par le bas) ; vérifié par `ui.pw.ts` et le fixture IPC (régions onglet/verre) ; rendu natif à observer |
| UI-010 | Streaming saccadé (retour de Lucas du 10/09 : « pas fluide, low quality », il préfère une roue puis le texte) | Traduction longue | Attente fluide, résultat d’un bloc, aucun à-coup | Cause : chaque delta (32 ms) faisait grandir le DOM puis la fenêtre native (`SetWindowPos` + `SetWindowRgn` + resize du HWND WebView2), recomposition synchrone impossible à interpoler. Correctif : deltas bufferisés dans `useTranslation`, anneau (`.loading-ring`, rotation composée GPU), un seul `resize` quand tout le texte arrive, verre qui s’ouvre en `clip-path` et texte en fondu (DOM pur). Vérifié par `ui.pw.ts` et le fixture IPC (un seul `resize_overlay` après `done`) |
| UI-011 | Ctrl+Alt+T redemande avant de traduire | Sélection dans une app sans UIA (repli presse-papiers) | Traduction immédiate à chaque pression | Confirmation supprimée ; le raccourci capture toujours la sélection courante au lieu de donner le focus. Vérifié par `ui.pw.ts` et `reducer.test.ts` |
| UI-012 | Bords « coupés au cutter », crénelés, ombres absentes (retour de Lucas, 10/09/2026) | Regarder un coin du verre, de la pilule ou de l’onglet à 100 % | Coins lissés, ombres visibles, clics à côté du verre qui traversent | Cause : `SetWindowRgn` (masque GDI 1 bit, sans alpha) découpait la boîte exacte du DOM : DWM tranche sans lissage, l’arc GDI ne coïncide pas avec l’arc Skia, toute ombre hors région disparaît. La région ne servait qu’au hit-test. Correctif : plus de région ; Chromium peint la silhouette (alpha par pixel via `DwmEnableBlurBehindWindow` de Tao) ; fenêtre agrandie d’un halo pour les ombres ; `host::start_hit_tester` sonde le curseur toutes les 8 ms et bascule `WS_EX_TRANSPARENT \| WS_EX_LAYERED` selon les régions publiées (spike validé par `WindowFromPoint` sur l’onglet réel). Le probe vérifie région absente, traversée du halo et prise du verre ; rendu à confirmer à l’œil |
| UI-007 | Titre « FlowTranslate » et coins carrés sur la bulle et la capsule après un clic sur la capsule (retour de Lucas en conditions réelles, 2026-09-10) | Capture presse-papiers → clic sur la capsule → observer la bulle, puis Fermer et recapturer | Aucune barre de titre DWM ; région arrondie conservée après activation, fermeture et nouvelle capture | Reproduit sur le build du 09-09 avec `scripts/inspect-native-windows.ps1` : région perdue après `focus_overlay`, styles CAPTION/SYSMENU rétablis après `hide`. Cause établie : `focus_overlay` passait par `show()`/`set_focus()` de Tao, dont `apply_diff` reconstruit les styles avec WS_CAPTION de façon différée (titre peint par DWM sur l’acrylique) et fait tomber la région. Correctif `host::activate` / `host::repair_handle` : activation et masquage au niveau HWND, réparation du style et de la région au focus. Le probe vérifie désormais styles vides et région pleine après affichage et après `focus_overlay` (`release/ui-evidence/native-after-frameless/result.json`) |
| UI-013 | Anneau d’attente trop rapide (vidéo de Lucas du 13/09/2026, 1 s) | Lancer une traduction, regarder l’anneau | Rotation lente et régulière (environ 1,4 s par tour), arc qui respire, rien d’autre ne bouge | Corrigé le 13/09/2026 (0.1.7) : `ring-spin` 1 400 ms linéaire, arc qui respire de 20 à 44 sur 1 200 ms (`ring-breathe`, aller-retour), trait 2,25 px ; durées vérifiées par `e2e/ui.pw.ts`, référence visuelle `pending` régénérée après inspection. Rythme à juger à l’œil par Lucas |
| UI-014 | Texte mal cadré dans le verre : un chemin coupé au milieu d’un mot (« …flowtranslate\r » puis « elease… »), bloc de texte rectangulaire au ras des arrondis (vidéo, 2 s) | Traduire une phrase contenant un chemin ou une URL sans espace | Coupure seulement après un séparateur (`\` `/` `-` `_` `.`), aucune lettre isolée au bord, marge latérale qui respecte le rayon | Corrigé le 13/09/2026 (0.1.7) : `breakable()` (`src/text.ts`, Vitest) place un `<wbr>` après `\` `/` `_` `-` `?` `&` `.` dans toute séquence sans espace d’au moins 18 caractères, résultat et original ; `overflow-wrap: break-word` en secours ; marge 22 px (24 px en lecteur). Le pont IPC vérifie qu’aucun morceau sans espace n’est coupé sur deux lignes (`e2e/native-bridge.pw.ts`) |
| UI-015 | Menu ⋯ gris et terne, survol plat, aucune transparence (vidéo, 3 s et 25 s) | Ouvrir le menu, survoler les entrées | Menu dans le même graphite que le verre, survol fondu en 120 ms, contraste net | Corrigé le 13/09/2026 (0.1.7), design seul : `--menu-bg` rgba(24,26,31,.96) avec le sheen du verre, bordure et séparateur à 8 %, entrées 8 × 12 px, surbrillance 8 % fondue en 120 ms ; référence visuelle `menu` régénérée. Le menu tient dans la réserve de 236 px de la fenêtre (six entrées au plus, vérifié par `e2e/ui.pw.ts`) |
| UI-016 | Bandeau de titre classique « FlowTranslate » peint en haut de la fenêtre de la bulle quand une autre fenêtre prend ou rend le focus (Réglages ouverts à 9,7 s, fermés à 15,9 s) ; reste environ 1 s, jusqu’au redimensionnement suivant | Ouvrir puis fermer les Réglages depuis le menu ; filmer le bord haut de la fenêtre à 30 i/s | Aucun bandeau, quel que soit le changement d’activation, verre ouvert ou replié | **Reproduit et cause établie le 13/09/2026** (`release/ui-evidence/band-repro/`) : le style reste sans WS_CAPTION ; `SendMessage(WM_NCACTIVATE, FALSE, 0)` seul peint le bandeau, le même message avec lParam = -1 ne peint rien, `WM_NCPAINT` et `WM_NCUAHDRAWCAPTION` seuls ne peignent rien. Mécanisme : Tao transmet `WM_NCACTIVATE` à `DefWindowProc` (`event_loop.rs`, `ProcResult::DefWindowProc`), dont le repeint par défaut dessine une barre de titre basique dans la surface de la fenêtre ; rien ne la repeint avant un resize. **Corrigé (0.1.7)** : `host::silence_frame` sous-classe le HWND de la bulle et de la capsule (`SetWindowSubclass`, appelé avant Tao) et transmet `WM_NCACTIVATE` avec lParam = -1, ignore `WM_NCPAINT`, `WM_NCUAHDRAWCAPTION` et `WM_NCUAHDRAWFRAME`. Preuve : `scripts/inspect-native-windows.ps1 -Poke` envoie `WM_NCACTIVATE(FALSE, 0)` à la fenêtre et compare la bande haute avant/après (contrôle « frame silent » du probe, sauté si la session est verrouillée). **Vérifié le 13/09/2026 sur le build 0.1.7** : probe PASS (`release/ui-evidence/native-after-fluidity`, différence moyenne 0 sur 2 184 échantillons, rect inchangé) ; rejeu du protocole (`release/ui-evidence/band-replay-0.1.7`) : scénario L différence 0 (bandeau franc sur 0.1.6), Réglages ouverts puis fermés sous le watcher : 47 captures, luminance de la bande haute constante, seuls les changements de premier plan sont journalisés, un seul rect de fenêtre. Reste le jugement à l’œil de Lucas |
| UI-017 | Original difficile à lire (vidéo, 25 s) | Menu ⋯ → Afficher l’original | Contraste suffisant, taille lisible, même rythme que la traduction | Corrigé le 13/09/2026 (0.1.7) : `.original-copy` 14 px à 85 %, interligne 1,45, fond rgba(255,255,255,.05), étiquette 11 px à 55 % ; vérifié par `e2e/ui.pw.ts`. Contraste à juger à l’œil par Lucas |
| UI-018 | Après « Agrandir », le verre se replie en onglet environ 0,8 s plus tard sans que la souris ait bougé ; il faut revenir sur l’onglet pour le rouvrir ; entrée et sortie brutales (vidéo, 25 à 27 s) | Menu ⋯ → Agrandir sans déplacer la souris | Après toute action, le verre reste ouvert au moins 2 s ; le repli attend une vraie sortie du pointeur ; repli et dépli animés | Corrigé le 13/09/2026 (0.1.7) : deux secondes de grâce datées par toute action (menu, pilule, clic dans la bulle) avant tout repli ; sortie jugée sur la silhouette (le sondeur natif émet `glass-near` quand le curseur entre ou sort d’une zone de 32 px autour des surfaces publiées) ; la fenêtre est réservée une fois (484 × 758 ancrée en bas, plancher 334 ancrée au texte) et le repli, le dépli et le menu ne changent plus que les surfaces (`host::set_regions`, aucun `SetWindowPos`) ; le corps reste monté et fond en 100 ms / 180 ms (`inert`, puis `visibility: hidden`). Vérifié par `e2e/ui.pw.ts`, `e2e/native-bridge.pw.ts` (une seule taille de fenêtre entre repli, dépli et menu) et `e2e/reported-defects.pw.ts` (atelier) ; probe natif du 13/09/2026 : fenêtre ancrée de 364 × 334 identique avant et après l’ouverture du menu sur trois cycles. Reste le jugement à l’œil de Lucas |

## Fiche d’une correction

- ID et scénario ; commit de départ.
- Observé / attendu ; hypothèse technique séparée.
- Reproduction exacte : fenêtre, taille, thème, gestes.
- Capture avant ; correction limitée ; capture après dans les mêmes conditions.
- Interaction rejouée, état final et régressions vérifiées.
- Animations : observation en mouvement et profil de performance séparés des captures fixes.
- Statut de revue esthétique par Lucas, sans l’assimiler aux tests automatiques.

## Ordre

1. UI-001 et UI-006 : fiabilité/observation native.
2. UI-002 : capsule et transitions au survol.
3. UI-003 / UI-004 : résultat court et long.
4. UI-005 : application et historique.
